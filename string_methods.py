# Some String Methods
# len : a function which takes an object as a parameter
name = 'I love Python' 
print(len(name)) # Spaces count # len is polymorphic; can be used on other data types
print(name.split('v')) # A method that runs on strings
pieces = ['I', 'l', 'ov', 'e', 'Pyth', 'on!']
print('---'.join(pieces))